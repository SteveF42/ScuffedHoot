const route = require('express').Router();
const database = require('../Database/MongoDB');


//add the actual game stuff here, will be added later
route.get('/', async(req,res)=>{

})

module.exports = route